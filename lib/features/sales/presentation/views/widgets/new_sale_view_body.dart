import 'package:flutter/widgets.dart';

class NewSaleViewBody extends StatelessWidget {
  const NewSaleViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        
      ],
    );
  }
}