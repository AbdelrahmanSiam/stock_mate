import 'package:flutter/material.dart';

class SalesFilterTabs extends StatelessWidget {
  const SalesFilterTabs({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}