import 'dart:ui';

abstract class AppColors {
  static const Color background = Color(0xFF0A0E1A); // Main screen background
  static const Color surface = Color(0xFF141829); // Cards, bottom nav, modals
  static const Color border = Color(0xFF252A3D); // Input borders, dividers
  static const Color primary = Color(0xFFFF6B2C); // Buttons, active states, FAB
  static const Color secondary = Color(0xFFFFB800); // Gradient accent, badges
  static const Color textPrimary = Color(0xFFFFFFFF); // Headlines, values
  static const Color textSecondary = Color(0xFF8A8FA8); // Hints, labels, captions
  static const Color error = Color(0xFFFF4757); // Validation, low stock, delete
  static const Color success = Color(0xFF2ED573); // Sale confirmed, in stock
}
